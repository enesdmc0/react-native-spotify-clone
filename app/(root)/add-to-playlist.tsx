import { View, Text } from "react-native";

const AddToPlaylistPage = () => {
  return (
    <View className="flex-1 flex items-center justify-center">
      <Text>AddToPlaylist Page</Text>
    </View>
  );
};

export default AddToPlaylistPage;
